# These things are run when an Openbox X Session is started.

# apps to install
# apt-get install nitrogen tint2 thunar xfce4-power-manager xfce4-volumed clipit pavucontrol
#
# also need pnmixer; compile from source https://github.com/nicklan/pnmixer

# If you want to use GNOME config tools...
#
if test -x /usr/lib/openbox/gnome-settings-daemon >/dev/null; then
  /usr/lib/openbox/gnome-settings-daemon &
elif which gnome-settings-daemon >/dev/null; then
  gnome-settings-daemon &
fi

# If you want to use XFCE config tools...
#
#xfce-mcs-manager &

tint2 &

# Wallpaper
nitrogen --restore &

# Compositor?

# volume keys - handled by gnome stuff already?
# xfce4-volumed &  

# volume applet
(sleep 4; pnmixer & )

# file manager
thunar &

# clipboard manager
clipit &

# no bell
xset b off

# not sure
# eval $(gnome-keyring-daemon -s --components=pkcs11,secrets,ssh,gpg) &
# xmodmap $HOME/.Xmodmap

if [ `hostname` = "sepulchre" ]; then
    # battery applet
    xfce4-power-manager &
fi

