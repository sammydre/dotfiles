color desert
if ( hostname() == 'stj-desktop' )
  set guifont=Envy\ Code\ R\ 9
else
  set guifont=Bitstream\ Vera\ Sans\ Mono\ 9
endif
set guioptions=r
" If we want to remove the scroll bars, we could remove "lr" from the
" guioptions too.
